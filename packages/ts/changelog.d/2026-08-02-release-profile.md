**Changed** — the published native artifacts are now stripped and built with
fat LTO, shrinking the `dirsql.node` addon ~28% (6.97 MB → 5.05 MB) and the
platform CLI binary ~33% (9.17 MB → 6.14 MB).

Build-only: no API or behavior change.
