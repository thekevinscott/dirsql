**Changed** — the napi binding crate now enforces the clippy cast lints (`cast_lossless`, `cast_possible_truncation`, `cast_possible_wrap`, `cast_precision_loss`, `cast_sign_loss`) via `[lints.clippy]`. String-length conversions to the napi_sys `isize` parameters go through a checked helper, and the guarded JS-number-to-integer cast is documented with `#[expect]`. No observable behavior change.
