**Fixed** — napi addons stage into `build/<triple>/`, the directory the release engine packages per-platform artifacts from. They were going to `build/napi-<triple>/`, a prefix that was only required while the npm family had two build rows; once the `bundled-cli` row was removed the upload matched nothing, so every npm platform artifact shipped empty and publishing failed its completeness check while the build jobs still reported success (#788).
