**Changed** — A duplicate table name now raises with a message naming the table **and both definition sites** (`Table 'dup' is defined by both a programmatic table and config /proj/dirsql.toml`); when both sides share an origin it reads `defined twice by <source>`. Previously the message was `duplicate table name: dup`, which named the table but neither definition. The exception type is unchanged.
