# Source and build artifacts
src/
ts/
target/
Cargo.*
build.rs
*.rs
tsconfig.json
# napi-rs emits `index.d.ts` at the package root as a build side effect;
# the published types come from dist/index.d.ts (see package.json `types`).
/index.d.ts

# Dev/CI infrastructure
.claude/
.github/
test/
vitest.config.ts

# Docs and notes
AGENTS.md
SUMMARY.md
notes/

# Build tools
justfile
