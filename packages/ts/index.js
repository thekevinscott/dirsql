// This file is auto-generated by @napi-rs/cli during build.
// Run `pnpm build` to regenerate it.
"use strict";

try {
  module.exports = require("./dirsql.node");
} catch (e) {
  // Fallback: try platform-specific binary
  const { platform, arch } = process;
  const platformArch = `${platform}-${arch}`;
  const triples = {
    "linux-x64": "dirsql.linux-x64-gnu.node",
    "linux-arm64": "dirsql.linux-arm64-gnu.node",
    "darwin-x64": "dirsql.darwin-x64.node",
    "darwin-arm64": "dirsql.darwin-arm64.node",
    "win32-x64": "dirsql.win32-x64-msvc.node",
  };
  const binding = triples[platformArch];
  if (binding) {
    module.exports = require(`./${binding}`);
  } else {
    throw new Error(
      `dirsql: unsupported platform ${platformArch}. Run \`pnpm build\` first.`
    );
  }
}
