[package]
name = "dirsql-napi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
dirsql-core = { path = "../core" }
napi = { version = "3", features = ["napi6", "serde-json"] }
napi-derive = "3"
regex = "1"
serde_json = "1"

[build-dependencies]
napi-build = "2"
