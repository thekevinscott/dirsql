# Migrations — TypeScript SDK

This file is a pointer, not the record — **never append entries here.**

Migration entries for this package live as one file per breaking change under
[`migrations.d/`](./migrations.d/), each a complete five-subsection entry.
Nothing is assembled back into this file.

Pre-fragment history (all packages) is frozen in the repository-root
[`MIGRATIONS.md`](../../MIGRATIONS.md).
